[package]
name = "rs-dataframe"
version = "0.1.0"
edition = "2021"
description = "minimal dataframe implementation in rust — csv, filter, groupby, join"

[dependencies]
csv = "1.3"
serde = { version = "1.0", features = ["derive"] }
